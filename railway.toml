[build]
builder = "nixpacks"

[build.buildCommand]
value = "npm ci && npm run build && npx prisma generate"

[deploy]
startCommand = "npm run start:prod"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
