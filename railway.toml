[build]
builder = "nixpacks"

[build.buildCommand]
value = "npm ci && npx prisma generate"

[deploy]
startCommand = "node dist/src/main"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
