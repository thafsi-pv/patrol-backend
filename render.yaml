services:
  - type: web
    name: patrol-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm ci && npx prisma generate
    startCommand: node dist/src/main
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
